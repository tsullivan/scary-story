'use strict';

var React = require('react');
var Stables = React.createClass({
  render() {
    return <div>
        <p>There once were stained glass windows in this place, but they're now broken and the full moon outside shines brightly through.</p>
      </div>
  }
});

module.exports = Stables;
